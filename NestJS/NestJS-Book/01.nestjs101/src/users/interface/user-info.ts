// Interface: 인터페이스는 일반적으로 타입체크를 위해 사용되며 변수, 함수 클래스에 사용할 수 있다.
// 인터페이스는 여러가지 타입을 갖는 프로퍼티로 이뤄진 새로운 타입을 정의하는 것과 유사.
// https://poiemaweb.com/typescript-interface


export interface UserInfo{
    id: string;
    name: string;
    email: string;
}